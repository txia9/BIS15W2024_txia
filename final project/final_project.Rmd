---
title: "final_project"
output: html_document
date: "2024-02-29"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
install.packages("naniar")
```

Libraries 
```{r}
library("tidyverse")
library("janitor")
library("naniar")
library("ggplot2")
```


Import data
```{r}
happiness <- read.csv("data/2022.csv") %>% clean_names()
```

1. find another dataset that group the countries in happiness dataset. 
2. use gapminder in lab12 hw. 
3. find the explanation sheet for each of the dataset (what the colomn means in each dataset.)
4. if possible, find a map of world, use alpha of color to show the happiness group by country
Max and Min. 

